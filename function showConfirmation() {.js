function showConfirmation() {
    var overlay = document.getElementById("overlay");
    overlay.style.display = "flex";
    setTimeout(() => {
        // Add code to open the Spotify playlist or redirect to the playlist URL
        // For example, window.location.href = 'your_spotify_playlist_url';
    }, 3000); // Show effects for 3 seconds, then trigger the playlist opening
}